info:
  name: Create
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/uploaders"
  body:
    type: json
    data: |-
      {
        "name": "Wesh"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
