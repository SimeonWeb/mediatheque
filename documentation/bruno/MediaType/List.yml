info:
  name: List
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/media_types"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
